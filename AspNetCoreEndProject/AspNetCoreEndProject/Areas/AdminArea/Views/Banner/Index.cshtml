@model IEnumerable<Banner>
@{
    ViewData["Title"] = "Index";

}

<div class="container-fluid my-3 mx-3">
    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title text-danger">Banner</h4>
                <div class="create-btn mb-3">
                    <a asp-area="AdminArea" asp-controller="Banner" asp-action="Create" class="btn btn-success"><i class="fa-solid fa-plus"></i></a>
                </div>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                            <tr>

                                <th>
                                    Image
                                </th>

                                <th>
                                    SubTitle
                                </th>
                                <th>
                                    Title
                                </th>
                                <th>
                                    Settings
                                </th>
                            </tr>

                        </thead>
                        <tbody>
                            @foreach (var banner in Model)
                            {
                                <tr class="table-info">

                                    <td>
                                        <img style="width:150px; height:150px;" src="~/assets/img/banner/@banner.Image" alt="slider" />
                                    </td>
                                    
                                    <td>
                                        @Html.Raw(banner.SubTitle)
                                    </td>
                                    <td style="width:350px">
                                        @Html.Raw(banner.Title)
                                    </td>
                                    <td id="parent-image" style="text-align: center;">
                                        <a asp-action="Detail" asp-route-id="@banner.Id" class="btn btn-info"><i class="mdi mdi-information mx-0 text-white"></i></a>
                                        <a asp-action="Edit" asp-route-id="@banner.Id" class="btn btn-primary"><i class="mdi mdi-table-edit"></i></a>
                                        <form id="parent-image" style="display: inline-block;" asp-action="Delete" asp-route-id="@banner.Id" method="post">
                                            <button data-id="@banner.Id" type="submit" class="btn btn-danger"><i class="mdi mdi-delete-forever"></i></button>
                                        </form>
                                    </td>
                                </tr>
                            }


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
